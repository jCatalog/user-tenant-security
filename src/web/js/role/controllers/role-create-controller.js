define(['role/role-module'], function (roleModule) {
    'use strict';
    roleModule.controller('RoleCreateController', ['$scope', function ($scope) {
        console.log('Role Create Controller running........');
    }]);
});
