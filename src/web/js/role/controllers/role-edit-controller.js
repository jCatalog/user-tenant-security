define(['role/role-module'], function (roleModule) {
    'use strict';
    roleModule.controller('RoleEditController', ['$scope','$state', function ($scope, $state) {
        console.log('Role Edit Controller running........');
    }]);
});
