define(['role/role-module'], function (roleModule) {
    'use strict';
    roleModule.controller('RoleDetailController', ['$scope', function ($scope) {
        console.log('Role Detail Controller running........');
    }]);
});
