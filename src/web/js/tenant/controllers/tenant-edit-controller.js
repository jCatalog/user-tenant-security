define(['tenant/tenant-module'], function (tenantModule) {
    'use strict';
    tenantModule.controller('TenantEditController', ['$scope','$state', function ($scope, $state) {
        console.log('Tenant Edit Controller running........');
    }]);
});
