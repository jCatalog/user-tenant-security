define(['tenant/tenant-module'], function (tenantModule) {
    'use strict';
    tenantModule.controller('TenantListController', ['$scope', function ($scope) {
        console.log('Tenant List Controller running........');
    }]);
});
