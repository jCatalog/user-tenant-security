define(['tenant/tenant-module'], function (tenantModule) {
    'use strict';
    tenantModule.controller('TenantController', ['$scope', function ($scope) {
        console.log('Tenant Controller running........');
    }]);
});
