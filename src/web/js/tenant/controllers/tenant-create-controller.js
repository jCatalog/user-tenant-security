define(['tenant/tenant-module'], function (tenantModule) {
    'use strict';
    tenantModule.controller('TenantCreateController', ['$scope', function ($scope) {
        console.log('Tenant Create Controller running........');
    }]);
});
