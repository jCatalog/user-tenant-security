define(['user/user-module'], function (userModule) {
    'use strict';
    userModule.controller('UserListController', ['$scope', function ($scope) {
        console.log('User List Controller running........');
    }]);
});
