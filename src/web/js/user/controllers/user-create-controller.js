define(['user/user-module'], function (userModule) {
    'use strict';
    userModule.controller('UserCreateController', ['$scope', function ($scope) {
        console.log('User Create Controller running........');
    }]);
});
