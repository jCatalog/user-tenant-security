define(['user/user-module'], function (userModule) {
    'use strict';
    userModule.controller('UserDetailController', ['$scope', function ($scope) {
        console.log('User Detail Controller running........');
    }]);
});
