define(['user/user-module'], function (userModule) {
    'use strict';
    userModule.controller('UserEditController', ['$scope', function ($scope) {
        console.log('User Edit Controller running........');
    }]);
});
